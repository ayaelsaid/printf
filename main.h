#ifndef MAIN_H
#define MAIN_H

#include <stdlib.h>
#include <stdarg.h>
#include <unistd.h>

int _putchar(char c);
int _puts(const char *str);
int _percent(void);
int _printf(const char *format, ...);
int fp_intger(int n);

#endif
